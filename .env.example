# Supabase Configuration
# These are automatically configured by Lovable Cloud - DO NOT edit manually
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key
VITE_SUPABASE_PROJECT_ID=your-project-id

# Edge Function Secrets (Configured via Lovable Cloud)
# These secrets are managed securely and should not be committed to version control
# - LOVABLE_API_KEY: API key for Lovable AI Gateway (auto-configured)
# - SUPABASE_URL: Supabase project URL (auto-configured)
# - SUPABASE_ANON_KEY: Supabase anonymous key (auto-configured)
# - SUPABASE_SERVICE_ROLE_KEY: Supabase service role key (auto-configured)
# - SUPABASE_DB_URL: Direct database connection URL (auto-configured)

# Security Notes:
# 1. All secrets are encrypted at rest
# 2. Never commit actual secret values to git
# 3. Rotate keys regularly through Lovable Cloud backend interface
# 4. Use service role key only in edge functions, never in client code
# 5. LOVABLE_API_KEY provides access to AI models without external API keys
